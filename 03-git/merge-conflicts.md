GitHub
   │
   ├── You deleted some folders/files
   │
Local Ubuntu DevOps Box
   │
   ├── You created new files
   │
Git notices:

"These histories don't match."

        ↓

git pull --rebase

        ↓

Conflict!

        ↓

You manually resolved it.

        ↓

git rebase --continue

        ↓

git push

        ↓

GitHub and Local are synchronized again.
